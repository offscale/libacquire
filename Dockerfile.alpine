FROM alpine

COPY . /libacquire

WORKDIR /libacquire/build

RUN apk add --no-cache gcc cmake make pkgconf musl-dev linux-headers libcurl curl-dev openssl-dev && \
    cmake -DCMAKE_BUILD_TYPE=Debug .. && \
    cmake --build .

CMD ctest .
